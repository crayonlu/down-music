# Environment example for front-end and MediaProxy
# Copy this file to .env (frontend) or use environment variables through your deployment tool.
# 前端和MediaProxy的环境示例
# 将此文件复制到 .env（前端）或通过部署工具使用环境变量。

# Full media proxy URL (optional). If set, frontend will use this URL directly.
# Example: https://api.example.com/proxy
# 完整媒体代理 URL（可选）。如果设置，前端将直接使用此 URL。
# 示例：https://api.example.com/proxy
VITE_MEDIA_PROXY_URL=

# Media proxy prefix (optional) when VITE_MEDIA_PROXY_URL is not set.
# Example: /api => proxy path will be https://your-site.com/api/proxy
# 未设置 VITE_MEDIA_PROXY_URL 时媒体代理前缀（可选）。
# 示例：/api => 代理路径为 https://your-site.com/api/proxy
VITE_MEDIA_PROXY_PREFIX=

# Enable proxy for images: true|false
# Default: false (do not proxy images)
# 启用图像代理：true|false
# 默认值： false （不代理图像）
VITE_PROXY_IMAGES=false

